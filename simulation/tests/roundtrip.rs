/*extern crate simulation;
use simulation::{Simulation, SimulationHandle};
use tokio;
use tokio_util::codec::{Framed, LinesCodec};

async fn server() {}

fn main() {
    let sim = Simulation::new(0);
    assert!(false);
}
*/
